export class Something {
    sum(a: number, b: number): number {
        return a + b;
    }
}

class Inject {}
class Component {
    constructor({selector:string}) {
        this.selector = selector;
    }
}
